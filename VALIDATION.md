# 候选工具的验证边界

2026-09-07。这些检查用于决定工具是否值得保留，不是获奖预测，也没有替学生完成人工理解与核验。

| 实际检查 | 结果与能说明什么 |
|---|---|
| Python 3.7.0 与 3.13.5，各运行 25 项脚本测试 | 两环境均 24 项通过，普通符号链接一项因 Windows 权限跳过；真实 junction 拒绝测试通过。覆盖成果变动、单位、口径、路径、Unicode、安装与更新保护。前版误记旧解释器为3.8，本次按实际版本更正。 |
| 新目录运行离线三克隆练习 | 约 3 秒完成；两个 bundle 合并无文本冲突，旧输入对应的结果凭据仍被拒绝。再次使用同一输出目录被拒绝。 |
| 未看搭建过程的独立 Codex 接手 | 发现条件变化，实际重算，另写约束复核，保留旧结果并修正说明。是代理模拟，不是三位学生或三真实账号测试。 |
| 独立审查：入口相同而被调用模块不同 | 漏登记模块时无法发现差异；登记后拒绝比较。工具不能自动证明登记范围完整，因此 Skill 明确要求关键模块与配置纳入。 |
| 只改变评价代码注释 | 保守拒绝自动比较。字节不同不必然意味着数学不同，需要读差异或统一复算；没有跳过检查的自动强制选项。 |
| 4 个阶段决策案例，有/无 Skill 的独立对照 | 两组均 4/4 正确，未观察到增益。不据此新增停止判定工具，也不声称提示已经解决长期自主工作过早停止。 |
| 完整 bundle → 新克隆 → 同 commit 安装两个 Skill | 实际通过，无网络；原 coach 的 9 个文件保持原字节，新 Skill 的 4 个文件随安装记录固定版本。 |
| 原生命令行启动的独立 Codex，只读安装检查 | 启动清单实际发现 modeling-team；随后文件读取命令被执行策略拒绝，未完成内容/commit 读取。自动发现成功不等于完整调用通过，未关闭策略绕过。 |
| 目标订单与历史组成不同的统计决策案例 | 独立执行者从数据建模、运行并解释正确，未收到组成偏差提示。未做基线对照，不据此增加新规则或宣称 Skill 增益。 |
| 真实往年题片段：独立求解与模型/实现/审查分工 | 分工初版在六种情形中的三种成本更高。查明一个不必要的策略绑定后做最小扩展，两条路线最佳成本一致；旧策略嵌入重算差为零。分工没有自动胜出，不能把一次模拟当三真实账号、真人理解或因果对照。 |
| 同一七年种植改进任务，有/无 Skill 的实际执行 | 同一初版 37,176,867.25 元，无 Skill 得 39,320,687.75 元，有 Skill 得 39,179,235.25 元；两组都继续改进，并通过原 Excel 独立复核。计算分别约 182 秒与 152 秒，未做重复试验，不推断 Skill 因果效果。没有观察到优化增益，也不能把它说成已解决长期自主停止问题。 |
| 独立写作者接收真实模型与结果文件 | 实际生成解释小节，并用另行转录参数的有理数闭式公式核查六代表方案；保留策略域、隐藏状态和人工未核验边界。说明本次接手成立，不等于三真实账号或真人测试。 |
| 实际种植候选打包后异目录接收 | 36,008字节完整bundle，克隆到中文含空格目录，Python3.13.5禁用site-packages仍复算出39,320,687.75元与574行。先暴露Git换行转换和仓库外bundle verify两个问题，修复后再克隆通过；这是同机不同目录演练，未冒充三账号。 |

可复查脚本测试：

```text
python -B -m unittest discover -s tests -p "test_*.py"
python examples/offline_drill.py --out ../my-team-drill
```

练习输出须为尚不存在的目录；接手测试见 [练习说明](examples/README.md)。这些测试不需要网络或第三方 Python 库，测试中的 Git 提交使用合成身份，不更改全局配置。

成果凭据主要减少“拿错版本、数值失去出处、不同声明口径直接排序”的风险。它不是科学验证器，不检查整个依赖图，不证明实际运行过、物理公式正确、最优性或真人审查。对缺失假设、错误评价模型、团队沟通和学生理解的帮助，仍需真实使用检验。

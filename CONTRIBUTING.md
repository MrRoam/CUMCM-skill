# 贡献与审阅约定

## 修改原则

对 Skill 的修改应解决真实使用问题，并保持范围集中。不要因单篇论文、一次失败或个人偏好而加入普遍性的绝对规则。

内容优先级为：用户明确要求、当年全国赛官方规则、适用的赛区或学校规则、任务证据、仓库中的经验建议。

## 修改流程

1. 在独立分支修改。
2. 说明修改针对的实际任务或失败现象。
3. 将详细指导放入对应的 `references/` 文件，避免扩大入口文件。
4. 运行 `python tools/validate_package.py`。
5. 用至少一个应触发和一个不应触发的真实提示词检查调用边界。
6. 由另一名队员复核后合并。

## 内容分类

- **官方要求**：必须能指向当年官方或适用的本地规则，并记录版本或日期。
- **样本规律**：来自优秀论文或统计观察，写明样本与局限。
- **建议**：解释为什么有帮助，同时保留作者选择权。
- **团队约定**：说明这是协作方式，不冒充竞赛规则。

## 必查项目

- 没有重复或近义重复规则；
- 没有未经证据支持的固定字数、图表数量、标题层级或配色要求；
- 没有放宽“不得编造数据、结果、引用”的边界；
- `SKILL.md` 能路由到所有必要参考文件；
- Python 脚本可解析，JSON 文件有效，本地 Markdown 链接存在；
- 没有论文原文、受限图片、个人路径、密钥或账号信息。

## 分支和提交

建议分支名使用 `skill/简短主题`，提交信息说明实际变化，例如：

```text
skill: clarify assumption validation review
```

一次提交尽量只处理一类变化，便于其他队员审核和回退。
